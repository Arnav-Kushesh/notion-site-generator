export default [
  {
    ignores: ["node_modules", "public", ".hugo_build_lock", "out", ".next", "notion_state", "styled-system"],
    rules: {
      "no-unused-vars": "warn",
      "no-console": "off"
    }
  }
];
